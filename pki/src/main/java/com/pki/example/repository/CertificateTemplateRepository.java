package com.pki.example.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CertificateTemplateRepository { //extends JpaRepository<CertificateTemplateRepository, Long>  {
}
